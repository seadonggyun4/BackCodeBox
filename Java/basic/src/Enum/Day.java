package EnumStudy;

// enum 은 변수의 값이 할당 가능한 범위를 지정한다.
// 할당가능한 범위를 미리 지정해두면 타입에 대해 지정된 범위 외의 값을 할당할수 없기때문에 안전하다.
// enum 은 switch case 문에서도 사용 가능하다.
public enum Day {
	SUNDAY,
	THUSDAY,
	WHENSEDAY,
	THIRSDAY,
	FRIYDAY,
}
